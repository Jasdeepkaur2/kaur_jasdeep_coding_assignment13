# Run locally

1. Build the app:
   npm run build

2. Build the Docker image:
   docker build -t kaur_jasdeep_coding_assignment12 .

3. Run the container:
   docker run -p 8083:8083 kaur_jasdeep_coding_assignment12

4. Open in browser:
   http://localhost:8083